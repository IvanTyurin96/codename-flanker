worker_processes  1;
events {
    worker_connections  1024;
}
http {
    include mime.types;
    
    server {
        listen 85;

        location / {
            root /etc/application/build;

            index    index.html;
        }
    }
}
